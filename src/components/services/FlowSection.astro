---
    import '../../css/services.css'
    import Flow from '../svg/services/Flow.astro';

    const { isMain } = Astro.props;
---

<section class="flow intro psai_flow_style">
    <div class="ctnr">
        <div class="graphic">
            <Flow/>
        </div>

        <div class="content slow_reveal">
            {isMain != "false" ? (
                <h1 class="title">
                    <b>
                        <span class="psai">psAI</span>
                        <span class="name">Flow</span>
                    </b>
                    <br/>
                    Optimising Data for AI Success
                </h1>
            ) : (
                <h2 class="title">
                    <b>
                        <span class="psai">psAI</span>
                        <span class="name">Flow</span>
                    </b>
                    <br/>
                    Optimising Data for AI Success
                </h2>
            )}
            
            <p>Data is the foundation of effective AI. psAI Flow focuses on <strong>refining your data management practices for seamless AI integration</strong>, turning data into a strategic asset for growth and innovation.</p>

            <div class="flex cta_ctnr">
                <a href={isMain != "false" ? "#learn-more" : "/services/flow#learn-more"} class="cta learn_more">Learn more</a>

                <button type="button" class="contact_bt">Contact us</button>
            </div>

            <!-- <p>psAI Define isn't merely an additional tool; it's the foundation for AI implementation. Here's why:</p>

            <ul class="cards">
                <li>
                    {isMain != "false" ? (
                        <h2>Custom AI Strategies</h2>
                    ) : (
                        <h3>Custom AI Strategies</h3>
                    )}

                    <p>Develop AI solutions that reflect your brand identity and business goals.</p>
                </li>
                
                <li>
                    {isMain != "false" ? (
                        <h2>Predictable Outcomes</h2>
                    ) : (
                        <h3>Predictable Outcomes</h3>
                    )}
                    
                    <p>Ensure consistent, reliable results from your AI investments.</p>
                </li>
                
                <li>
                    {isMain != "false" ? (
                        <h2>Brand Alignment</h2>
                    ) : (
                        <h3>Brand Alignment</h3>
                    )}
                    
                    <p>Align AI functionalities with your brand voice and customer expectations.</p>
                </li>
            </ul> -->
        </div>
    </div>
</section>